# Free key: https://developer.company-information.service.gov.uk/
COMPANIES_HOUSE_API_KEY=

# Optional. Leave blank to use the deterministic template reasoner (default, no
# external dependency). If set, the agent uses this to write the stakeholder-facing
# prose on top of the already-decided deterministic ranking (see DECISIONS.md #7).
ANTHROPIC_API_KEY=
